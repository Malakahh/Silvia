<Window x:Class="SilviaGUI.CmdTabCompletion"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CmdTabCompletion" Height="120"
        ShowInTaskbar="False"
        Topmost="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SilviaCore;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.Style>
        <StaticResource ResourceKey="SilviaWindow"/>
    </Window.Style>
    <Grid>
        <ListBox x:Name="TabCompletionList"
                 SelectionMode="Single"
                 Style="{StaticResource ResourceKey=SilviaListBox}"                 
                 ItemContainerStyle="{StaticResource ResourceKey=SilviaListBoxItem}">
            <ListBox.Resources>
                <Style TargetType="{x:Type ScrollBar}">
                    <Setter Property="Template" Value="{DynamicResource SilviaScrollBar}"/>
                </Style>
            </ListBox.Resources>
        </ListBox>
    </Grid>
</Window>
